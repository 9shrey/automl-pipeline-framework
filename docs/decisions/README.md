# Architecture Decision Records

Each ADR is a short doc capturing **context → decision → consequences**.
Indexed list lands as ADRs are written. Planned:

- 0001 — Sampler choice: TPE primary, ASHA pruner.
- 0002 — Meta-feature set (~30 features, statistical + landmarking).
- 0003 — Default ensemble = stacking with OOF predictions.
- 0004 — Meta-store backend = SQLite + Parquet artifacts.
- 0005 — Package manager = uv; build backend = hatchling.
